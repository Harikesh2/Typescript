let greeting: string = "Hello Harikesh";


// number is for numbers like 42. JavaScript does not have a special runtime value for integers, so there’s no equivalent to int or float - everything is simply numbernumber is for numbers like 42. JavaScript does not have a special runtime value for integers, so there’s no equivalent to int or float - everything is simply number

//   not a good practice
let userId : number = 212345.2345;

// good Practice

let studentId = 13;

// Any Example of this case 

let hero;

function getHero(){
    return "Iron-Man"
}

hero = getHero();

// You usually want to avoid this, though, because any isn’t type-checked. Use the compiler flag noImplicitAny to flag any implicit any as an error.





// boolean
let loggedIN: boolean = true;


greeting.toLowerCase();

console.log(greeting);



export {}