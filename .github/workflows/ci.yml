name: CI

on:
  push:
    branches: [main]
    paths:
      - 'CRM/Django-CRM/**'
  pull_request:
    branches: [main]
    paths:
      - 'CRM/Django-CRM/**'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: CRM/Django-CRM

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        run: python -m pytest